parameters:
    level: max
    paths:
        - src
    tmpDir: build/phpstan
    ignoreErrors:
        # The Paper trait is consumed by downstream models, never used within src itself.
        - identifier: trait.unused
